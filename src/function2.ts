let userDetails: (
  id: number | string,
  userInfo: {
    name: string;
    age: number;
  }
) => void;

userDetails = (
  id: number | string,
  user: {
    name: string;
    age: number;
  }
) => {};
